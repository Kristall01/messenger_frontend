import React from 'react';
import ReactDOM from 'react-dom';
import MessengerApp from './app/MessengerApp';

ReactDOM.render(
	<React.StrictMode>
		<MessengerApp />
	</React.StrictMode>,
	document.getElementById('root')
);
